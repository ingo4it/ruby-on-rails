<h1><%= @article.title %></h1>


<p><%= @article.body %></p>

<%= render @article.comments %>
<h2>Add Comment</h2>
<%= render 'comments/form' %>



<div><%= link_to "Articles", articles_path %></div>
<div><%= link_to "Edit", edit_article_path(@article) %></div>
<div><%= link_to "Destroy", article_path(@article), data: { turbo_method: :delete, turbo_confirm: 'Are you sure'} %></div>

